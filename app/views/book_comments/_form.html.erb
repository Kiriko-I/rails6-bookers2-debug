<%= form_with(model: [books, book_comment], local: false) do |f| %>
  <%= f.text_area :comment, rows: '5', class: 'form-control book_comment' %>
  <div class="new-comment"><%= f.submit "送信" %></div>
<% end %>

<script>
$(".new-comment").on('click',function(){
    location.reload();
});
</script>